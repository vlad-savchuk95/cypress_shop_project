{
  "name": "cypress_shop_project",
  "version": "1.0.0",
  "description": "discover best practices for effectively utilizing the Cypress framework",
  "main": "index.js",
  "scripts": {
    "ui-regression:chrome": "cypress run --spec 'cypress/e2e/**/*' --browser chrome",
    "ui-regression:electron": "cypress run --spec 'cypress/e2e/**/*' --browser electron",
    "install:dependencies": "npm install",
    "cypress:run": "cypress run"
  },
  "keywords": [],
  "author": "Vlad Savchuk",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^8.0.2",
    "cypress": "^13.2.0",
    "cypress-mochawesome-reporter": "^3.6.0",
    "faker": "^5.5.3",
    "mochawesome": "^7.1.3",
    "npm-run-all": "^4.1.5"
  }
}
